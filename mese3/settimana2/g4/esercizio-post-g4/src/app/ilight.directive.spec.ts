import { IlightDirective } from './ilight.directive';

describe('IlightDirective', () => {
  it('should create an instance', () => {
    const directive = new IlightDirective();
    expect(directive).toBeTruthy();
  });
});
